#! /bin/sh

myDir="`dirname \"$0\"`"
$myDir/build
rm -r $myDir/.casper
mkdir $myDir/.casper
echo "Installing casper..."
$myDir/nuget install Casper.Console -OutputDirectory $myDir/.casper -pre -Verbosity quiet
rm -r $myDir/out
$myDir/casper $@
