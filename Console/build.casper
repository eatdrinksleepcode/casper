import Casper;

task CreatePackagesDir:
	System.IO.Directory.CreateDirectory("../out/packages")

task Pack(Exec,
		  DependsOn: [Tasks['Compile'], CreatePackagesDir],
		  Executable: '..\\nuget.exe',
		  Arguments: 'pack Console.nuspec -OutputDirectory ../out/packages -Properties "Configuration=Release"')

parent.Tasks['Build'].DependsOn.Add(Pack)
