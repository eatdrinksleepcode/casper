import Casper;

task compile(MSBuild,
			 Properties: {"Configuration": "Release"})

task test(NUnit,
		  DependsOn: [compile],
		  TestAssembly: 'Test/bin/Release/Casper.Test.dll')

task build(DependsOn: [test])

include 'Console/build.casper'
